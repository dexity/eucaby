module: api
runtime: python27
api_version: 1
threadsafe: true

handlers:
- url: .*
  script: eucaby_api.wsgi.app

libraries:
- name: MySQLdb
  version: latest
- name: ssl
  version: latest

#inbound_services:
#- warmup

includes:
- common.yaml
