version: rt
runtime: custom
api_version: 1
threadsafe: true
vm: true

manual_scaling:
  instances: 1

vm_settings:
  machine_type: n1-standard-1

handlers:

- url: /.*
  script: main.application

skip_files:
- ^deploy*
- ^node_modules